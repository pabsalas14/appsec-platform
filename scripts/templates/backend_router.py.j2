"""{{ pascal }} CRUD endpoints — generated by scripts/new_entity.py."""

from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.deps import get_current_user, get_db
from app.api.deps_ownership import require_ownership
from app.core.response import success
from app.models.user import User
from app.models.{{ snake }} import {{ pascal }}
from app.schemas.{{ snake }} import {{ pascal }}Create, {{ pascal }}Read, {{ pascal }}Update
from app.services.{{ snake }}_service import {{ snake }}_svc

router = APIRouter()


@router.get("")
async def list_{{ plural_snake }}(
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    """List {{ plural_snake }} owned by the current user."""
    items = await {{ snake }}_svc.list(db, filters={"user_id": current_user.id})
    return success([{{ pascal }}Read.model_validate(x).model_dump(mode="json") for x in items])


@router.get("/{id}")
async def get_{{ snake }}(
    entity: {{ pascal }} = Depends(require_ownership({{ snake }}_svc)),
):
    """Get a single owned {{ snake }} by ID (404 if not owned)."""
    return success({{ pascal }}Read.model_validate(entity).model_dump(mode="json"))


@router.post("", status_code=201)
async def create_{{ snake }}(
    entity_in: {{ pascal }}Create,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    """Create a new {{ snake }} for the current user."""
    entity = await {{ snake }}_svc.create(db, entity_in, extra={"user_id": current_user.id})
    return success({{ pascal }}Read.model_validate(entity).model_dump(mode="json"))


@router.patch("/{id}")
async def update_{{ snake }}(
    entity_in: {{ pascal }}Update,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
    entity: {{ pascal }} = Depends(require_ownership({{ snake }}_svc)),
):
    """Partially update an owned {{ snake }} (404 if not owned)."""
    updated = await {{ snake }}_svc.update(
        db, entity.id, entity_in, scope={"user_id": current_user.id}
    )
    return success({{ pascal }}Read.model_validate(updated).model_dump(mode="json"))


@router.delete("/{id}")
async def delete_{{ snake }}(
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
    entity: {{ pascal }} = Depends(require_ownership({{ snake }}_svc)),
):
    """Delete an owned {{ snake }} (404 if not owned)."""
    await {{ snake }}_svc.delete(db, entity.id, scope={"user_id": current_user.id})
    return success(None, meta={"message": "{{ pascal }} deleted"})
